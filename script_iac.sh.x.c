#if 0
	shc Version 4.0.3, Generic Shell Script Compiler
	GNU GPL Version 3 Md Jahidul Hamid <jahidulhamid@yahoo.com>

	shc -f script_iac.sh -o servidor_web_apache2_for_deb_or_ubuntu 
#endif

static  char data [] = 
#define      xecc_z	15
#define      xecc	((&data[0]))
	"\310\230\203\070\300\110\154\255\335\040\251\264\014\213\355"
#define      rlax_z	1
#define      rlax	((&data[15]))
	"\043"
#define      msg1_z	65
#define      msg1	((&data[20]))
	"\262\212\173\210\236\157\047\004\201\070\010\107\317\370\363\301"
	"\130\201\120\101\042\027\123\030\275\064\326\253\302\321\031\271"
	"\240\033\030\125\241\060\306\032\215\065\030\030\332\062\155\114"
	"\220\277\063\107\053\204\341\167\101\027\143\131\207\277\146\050"
	"\362\332\020\310\302"
#define      chk1_z	22
#define      chk1	((&data[86]))
	"\213\241\346\375\132\342\304\161\141\340\161\143\162\163\221\161"
	"\056\113\154\162\125\371\022\141\033\025"
#define      pswd_z	256
#define      pswd	((&data[143]))
	"\170\037\254\364\353\137\334\041\104\123\131\261\320\252\033\200"
	"\055\214\263\304\236\041\214\167\334\156\211\133\230\002\351\053"
	"\336\206\222\265\234\230\043\063\344\101\171\206\041\330\175\372"
	"\214\052\326\221\353\131\231\224\036\317\110\154\376\310\332\021"
	"\242\116\215\051\161\205\334\365\226\146\331\205\227\327\131\261"
	"\247\005\142\237\313\303\230\343\154\017\246\170\051\150\274\241"
	"\304\101\030\053\230\023\241\064\032\004\115\310\330\241\122\217"
	"\320\325\011\022\200\261\275\033\074\334\022\300\231\033\171\004"
	"\370\155\117\323\027\327\263\051\157\370\317\107\024\102\021\041"
	"\346\270\007\011\201\034\263\010\030\265\045\042\227\037\331\170"
	"\006\330\335\311\375\131\143\216\133\240\272\272\364\272\332\363"
	"\307\002\302\030\000\212\232\360\207\344\014\044\262\020\152\177"
	"\345\234\342\134\242\250\341\173\000\057\031\007\067\332\070\240"
	"\253\174\333\133\167\072\340\247\373\167\011\045\360\273\343\254"
	"\020\040\053\153\306\337\310\263\377\121\042\121\366\250\210\347"
	"\321\303\016\054\110\130\145\354\253\171\264\134\265\110\026\134"
	"\147\053\105\300\112\113\110\354\101\333\172\024\263\314\334\021"
	"\226\375\101\067\011\145\264\146\342\012\333\024\206\347\301\224"
	"\376\025\242\275\240\304\360\001\374\201\245\141\043\035\363\276"
	"\313\333\012\246\361\023\207"
#define      date_z	1
#define      date	((&data[422]))
	"\200"
#define      lsto_z	1
#define      lsto	((&data[423]))
	"\234"
#define      tst2_z	19
#define      tst2	((&data[424]))
	"\354\272\017\001\020\307\243\141\256\042\363\116\216\173\325\144"
	"\322\366\025\373\002\262"
#define      opts_z	1
#define      opts	((&data[446]))
	"\100"
#define      shll_z	10
#define      shll	((&data[447]))
	"\367\053\332\243\140\000\266\222\260\326"
#define      chk2_z	19
#define      chk2	((&data[459]))
	"\272\267\330\242\235\122\073\353\036\134\014\234\075\053\346\000"
	"\023\376\065\013\220"
#define      inlo_z	3
#define      inlo	((&data[478]))
	"\162\154\337"
#define      tst1_z	22
#define      tst1	((&data[481]))
	"\024\101\076\151\021\126\045\014\076\153\331\074\250\222\233\311"
	"\315\102\215\374\276\041\145\113\127"
#define      msg2_z	19
#define      msg2	((&data[507]))
	"\217\233\027\305\234\306\140\023\030\313\261\246\215\337\251\165"
	"\242\212\307\066\012\001\276\166"
#define      text_z	586
#define      text	((&data[624]))
	"\243\027\213\247\034\222\165\307\361\217\260\100\222\052\213\231"
	"\312\054\370\113\042\045\256\240\032\071\367\142\307\005\213\017"
	"\030\264\005\131\100\034\262\240\362\277\047\372\303\220\230\330"
	"\111\050\231\242\326\100\371\043\017\363\240\131\103\164\354\232"
	"\133\251\044\267\202\351\040\372\300\203\232\225\362\336\056\340"
	"\256\260\143\023\064\350\341\021\233\056\146\130\122\053\167\160"
	"\374\043\312\162\270\266\367\131\324\271\010\341\311\100\065\270"
	"\324\133\156\233\133\331\167\252\372\123\227\056\050\311\022\326"
	"\362\343\347\211\146\147\272\255\067\025\317\343\302\376\145\164"
	"\234\015\212\042\325\335\103\352\034\201\203\071\354\026\377\147"
	"\017\167\335\211\335\151\116\312\155\166\304\256\207\346\171\256"
	"\374\004\336\246\277\057\306\240\041\256\061\250\044\020\317\155"
	"\276\332\256\254\113\161\170\000\056\233\146\141\006\177\145\343"
	"\253\226\024\250\215\147\012\367\053\374\347\344\074\316\042\375"
	"\157\117\337\357\353\145\236\245\330\301\227\102\010\052\061\112"
	"\140\060\200\371\166\010\141\067\217\011\177\304\357\264\203\235"
	"\320\067\147\104\106\005\042\347\125\155\260\043\173\144\036\161"
	"\057\271\163\026\060\175\360\005\373\115\146\164\016\305\236\124"
	"\146\331\221\313\262\306\132\110\255\022\372\056\245\100\321\023"
	"\363\303\032\317\054\266\162\147\317\001\215\322\105\062\330\055"
	"\337\351\145\055\204\060\105\065\375\324\305\003\245\024\350\132"
	"\372\007\031\033\200\146\030\367\152\244\010\310\301\021\261\315"
	"\025\375\214\202\146\170\171\247\214\160\107\315\054\350\040\002"
	"\277\347\130\002\020\000\164\176\064\072\077\064\214\074\201\227"
	"\230\155\231\104\175\000\116\045\023\043\325\205\074\126\240\310"
	"\117\220\125\004\112\247\014\356\225\353\066\011\350\205\156\223"
	"\137\073\174\104\276\312\055\006\041\067\232\244\030\055\256\274"
	"\034\104\007\162\141\373\027\236\163\212\247\347\217\126\060\141"
	"\027\253\250\126\372\006\226\366\000\230\164\042\354\355\044\306"
	"\044\137\303\160\331\263\126\140\176\322\336\302\151\136\062\230"
	"\073\153\367\341\022\103\222\353\225\334\371\251\356\020\327\171"
	"\300\014\153\315\056\227\317\054\206\017\375\260\015\267\245\345"
	"\236\155\365\251\247\272\271\111\276\216\324\264\351\357\147\156"
	"\075\221\372\153\076\313\306\070\314\150\062\112\361\072\050\263"
	"\116\112\102\260\004\262\211\264\113\225\277\157\011\031\361\273"
	"\210\166\032\314\317\164\243\314\203\211\210\343\032\246\146\100"
	"\240\215\264\074\163\101\010\321\032\023\026\013\204\040\273\044"
	"\010\144\045\243\312\375\265\216\277\231\176\012\040\322\251\115"
	"\314\173\224\051\103\205\155\333\224\041\244\343\237\143\214\102"
	"\373\151\060\027\377\154\224\120\127\173\040\054\177\355\273\244"
	"\270\325\213\316\156\112\150\147\246\266\320\100\346\137\307\141"
	"\103\265\345\266\023\172\233\371\320\114\012\036\263\325\130\054"
	"\103\265\144\356\043\027\367\362\236\304\167\024\224\341\255\265"
	"\103\175\032\217\264\221\336\211\271\010\250\272\300\212\343\022"
	"\235\055\334\371\002\032\024\176\112\013\112\165\055\046\334\246"
	"\161\147\101\251\317\250\166\032\060\351\151\003\115\000\210\377"
	"\155\274\010\265\134\140\042\045\276\020\112\137\314\160\170\237"
	"\225\054\274\031\227\130\352\362\070\121\103\170\037\243\206\166"
	"\010\153\125\277\256\370\367\343\010\354\250\272\377\114\103\302"
	"\130\056\023\343\345\352\330\227\015\076\305\236\030\227\376\200"
	"\176\135\102\370\207\324\237"/* End of data[] */;
#define      hide_z	4096
#define SETUID 0	/* Define as 1 to call setuid(0) at start of script */
#define DEBUGEXEC	0	/* Define as 1 to debug execvp calls */
#define TRACEABLE	1	/* Define as 1 to enable ptrace the executable */
#define HARDENING	0	/* Define as 1 to disable ptrace/dump the executable */
#define BUSYBOXON	0	/* Define as 1 to enable work with busybox */

#if HARDENING
static const char * shc_x[] = {
"/*",
" * Copyright 2019 - Intika <intika@librefox.org>",
" * Replace ******** with secret read from fd 21",
" * Also change arguments location of sub commands (sh script commands)",
" * gcc -Wall -fpic -shared -o shc_secret.so shc_secret.c -ldl",
" */",
"",
"#define _GNU_SOURCE /* needed to get RTLD_NEXT defined in dlfcn.h */",
"#define PLACEHOLDER \"********\"",
"#include <dlfcn.h>",
"#include <stdlib.h>",
"#include <string.h>",
"#include <unistd.h>",
"#include <stdio.h>",
"#include <signal.h>",
"",
"static char secret[128000]; //max size",
"typedef int (*pfi)(int, char **, char **);",
"static pfi real_main;",
"",
"// copy argv to new location",
"char **copyargs(int argc, char** argv){",
"    char **newargv = malloc((argc+1)*sizeof(*argv));",
"    char *from,*to;",
"    int i,len;",
"",
"    for(i = 0; i<argc; i++){",
"        from = argv[i];",
"        len = strlen(from)+1;",
"        to = malloc(len);",
"        memcpy(to,from,len);",
"        // zap old argv space",
"        memset(from,'\\0',len);",
"        newargv[i] = to;",
"        argv[i] = 0;",
"    }",
"    newargv[argc] = 0;",
"    return newargv;",
"}",
"",
"static int mymain(int argc, char** argv, char** env) {",
"    //fprintf(stderr, \"Inject main argc = %d\\n\", argc);",
"    return real_main(argc, copyargs(argc,argv), env);",
"}",
"",
"int __libc_start_main(int (*main) (int, char**, char**),",
"                      int argc,",
"                      char **argv,",
"                      void (*init) (void),",
"                      void (*fini)(void),",
"                      void (*rtld_fini)(void),",
"                      void (*stack_end)){",
"    static int (*real___libc_start_main)() = NULL;",
"    int n;",
"",
"    if (!real___libc_start_main) {",
"        real___libc_start_main = dlsym(RTLD_NEXT, \"__libc_start_main\");",
"        if (!real___libc_start_main) abort();",
"    }",
"",
"    n = read(21, secret, sizeof(secret));",
"    if (n > 0) {",
"      int i;",
"",
"    if (secret[n - 1] == '\\n') secret[--n] = '\\0';",
"    for (i = 1; i < argc; i++)",
"        if (strcmp(argv[i], PLACEHOLDER) == 0)",
"          argv[i] = secret;",
"    }",
"",
"    real_main = main;",
"",
"    return real___libc_start_main(mymain, argc, argv, init, fini, rtld_fini, stack_end);",
"}",
"",
0};
#endif /* HARDENING */

/* rtc.c */

#include <sys/stat.h>
#include <sys/types.h>

#include <errno.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <unistd.h>

/* 'Alleged RC4' */

static unsigned char stte[256], indx, jndx, kndx;

/*
 * Reset arc4 stte. 
 */
void stte_0(void)
{
	indx = jndx = kndx = 0;
	do {
		stte[indx] = indx;
	} while (++indx);
}

/*
 * Set key. Can be used more than once. 
 */
void key(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		do {
			tmp = stte[indx];
			kndx += tmp;
			kndx += ptr[(int)indx % len];
			stte[indx] = stte[kndx];
			stte[kndx] = tmp;
		} while (++indx);
		ptr += 256;
		len -= 256;
	}
}

/*
 * Crypt data. 
 */
void arc4(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		indx++;
		tmp = stte[indx];
		jndx += tmp;
		stte[indx] = stte[jndx];
		stte[jndx] = tmp;
		tmp += stte[indx];
		*ptr ^= stte[tmp];
		ptr++;
		len--;
	}
}

/* End of ARC4 */

#if HARDENING

#include <sys/ptrace.h>
#include <sys/wait.h>
#include <signal.h>
#include <sys/prctl.h>
#define PR_SET_PTRACER 0x59616d61

/* Seccomp Sandboxing Init */
#include <stdlib.h>
#include <stdio.h>
#include <stddef.h>
#include <string.h>
#include <unistd.h>
#include <errno.h>

#include <sys/types.h>
#include <sys/prctl.h>
#include <sys/syscall.h>
#include <sys/socket.h>

#include <linux/filter.h>
#include <linux/seccomp.h>
#include <linux/audit.h>

#define ArchField offsetof(struct seccomp_data, arch)

#define Allow(syscall) \
    BPF_JUMP(BPF_JMP+BPF_JEQ+BPF_K, SYS_##syscall, 0, 1), \
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_ALLOW)

struct sock_filter filter[] = {
    /* validate arch */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, ArchField),
    BPF_JUMP( BPF_JMP+BPF_JEQ+BPF_K, AUDIT_ARCH_X86_64, 1, 0),
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),

    /* load syscall */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, offsetof(struct seccomp_data, nr)),

    /* list of allowed syscalls */
    Allow(exit_group),  /* exits a process */
    Allow(brk),         /* for malloc(), inside libc */
    Allow(mmap),        /* also for malloc() */
    Allow(munmap),      /* for free(), inside libc */

    /* and if we don't match above, die */
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),
};
struct sock_fprog filterprog = {
    .len = sizeof(filter)/sizeof(filter[0]),
    .filter = filter
};

/* Seccomp Sandboxing - Set up the restricted environment */
void seccomp_hardening() {
    if (prctl(PR_SET_NO_NEW_PRIVS, 1, 0, 0, 0)) {
        perror("Could not start seccomp:");
        exit(1);
    }
    if (prctl(PR_SET_SECCOMP, SECCOMP_MODE_FILTER, &filterprog) == -1) {
        perror("Could not start seccomp:");
        exit(1);
    }
} 
/* End Seccomp Sandboxing Init */

void shc_x_file() {
    FILE *fp;
    int line = 0;

    if ((fp = fopen("/tmp/shc_x.c", "w")) == NULL ) {exit(1); exit(1);}
    for (line = 0; shc_x[line]; line++)	fprintf(fp, "%s\n", shc_x[line]);
    fflush(fp);fclose(fp);
}

int make() {
	char * cc, * cflags, * ldflags;
    char cmd[4096];

	cc = getenv("CC");
	if (!cc) cc = "cc";

	sprintf(cmd, "%s %s -o %s %s", cc, "-Wall -fpic -shared", "/tmp/shc_x.so", "/tmp/shc_x.c -ldl");
	if (system(cmd)) {remove("/tmp/shc_x.c"); return -1;}
	remove("/tmp/shc_x.c"); return 0;
}

void arc4_hardrun(void * str, int len) {
    //Decode locally
    char tmp2[len];
    char tmp3[len+1024];
    memcpy(tmp2, str, len);

	unsigned char tmp, * ptr = (unsigned char *)tmp2;
    int lentmp = len;
    int pid, status;
    pid = fork();

    shc_x_file();
    if (make()) {exit(1);}

    setenv("LD_PRELOAD","/tmp/shc_x.so",1);

    if(pid==0) {

        //Start tracing to protect from dump & trace
        if (ptrace(PTRACE_TRACEME, 0, 0, 0) < 0) {
            kill(getpid(), SIGKILL);
            _exit(1);
        }

        //Decode Bash
        while (len > 0) {
            indx++;
            tmp = stte[indx];
            jndx += tmp;
            stte[indx] = stte[jndx];
            stte[jndx] = tmp;
            tmp += stte[indx];
            *ptr ^= stte[tmp];
            ptr++;
            len--;
        }

        //Do the magic
        sprintf(tmp3, "%s %s", "'********' 21<<<", tmp2);

        //Exec bash script //fork execl with 'sh -c'
        system(tmp2);

        //Empty script variable
        memcpy(tmp2, str, lentmp);

        //Clean temp
        remove("/tmp/shc_x.so");

        //Sinal to detach ptrace
        ptrace(PTRACE_DETACH, 0, 0, 0);
        exit(0);
    }
    else {wait(&status);}

    /* Seccomp Sandboxing - Start */
    seccomp_hardening();

    exit(0);
}
#endif /* HARDENING */

/*
 * Key with file invariants. 
 */
int key_with_file(char * file)
{
	struct stat statf[1];
	struct stat control[1];

	if (stat(file, statf) < 0)
		return -1;

	/* Turn on stable fields */
	memset(control, 0, sizeof(control));
	control->st_ino = statf->st_ino;
	control->st_dev = statf->st_dev;
	control->st_rdev = statf->st_rdev;
	control->st_uid = statf->st_uid;
	control->st_gid = statf->st_gid;
	control->st_size = statf->st_size;
	control->st_mtime = statf->st_mtime;
	control->st_ctime = statf->st_ctime;
	key(control, sizeof(control));
	return 0;
}

#if DEBUGEXEC
void debugexec(char * sh11, int argc, char ** argv)
{
	int i;
	fprintf(stderr, "shll=%s\n", sh11 ? sh11 : "<null>");
	fprintf(stderr, "argc=%d\n", argc);
	if (!argv) {
		fprintf(stderr, "argv=<null>\n");
	} else { 
		for (i = 0; i <= argc ; i++)
			fprintf(stderr, "argv[%d]=%.60s\n", i, argv[i] ? argv[i] : "<null>");
	}
}
#endif /* DEBUGEXEC */

void rmarg(char ** argv, char * arg)
{
	for (; argv && *argv && *argv != arg; argv++);
	for (; argv && *argv; argv++)
		*argv = argv[1];
}

void chkenv_end(void);

int chkenv(int argc)
{
	char buff[512];
	unsigned long mask, m;
	int l, a, c;
	char * string;
	extern char ** environ;

	mask = (unsigned long)getpid();
	stte_0();
	 key(&chkenv, (void*)&chkenv_end - (void*)&chkenv);
	 key(&data, sizeof(data));
	 key(&mask, sizeof(mask));
	arc4(&mask, sizeof(mask));
	sprintf(buff, "x%lx", mask);
	string = getenv(buff);
#if DEBUGEXEC
	fprintf(stderr, "getenv(%s)=%s\n", buff, string ? string : "<null>");
#endif
	l = strlen(buff);
	if (!string) {
		/* 1st */
		sprintf(&buff[l], "=%lu %d", mask, argc);
		putenv(strdup(buff));
		return 0;
	}
	c = sscanf(string, "%lu %d%c", &m, &a, buff);
	if (c == 2 && m == mask) {
		/* 3rd */
		rmarg(environ, &string[-l - 1]);
		return 1 + (argc - a);
	}
	return -1;
}

void chkenv_end(void){}

#if HARDENING

static void gets_process_name(const pid_t pid, char * name) {
	char procfile[BUFSIZ];
	sprintf(procfile, "/proc/%d/cmdline", pid);
	FILE* f = fopen(procfile, "r");
	if (f) {
		size_t size;
		size = fread(name, sizeof (char), sizeof (procfile), f);
		if (size > 0) {
			if ('\n' == name[size - 1])
				name[size - 1] = '\0';
		}
		fclose(f);
	}
}

void hardening() {
    prctl(PR_SET_DUMPABLE, 0);
    prctl(PR_SET_PTRACER, -1);

    int pid = getppid();
    char name[256] = {0};
    gets_process_name(pid, name);

    if (   (strcmp(name, "bash") != 0) 
        && (strcmp(name, "/bin/bash") != 0) 
        && (strcmp(name, "sh") != 0) 
        && (strcmp(name, "/bin/sh") != 0) 
        && (strcmp(name, "sudo") != 0) 
        && (strcmp(name, "/bin/sudo") != 0) 
        && (strcmp(name, "/usr/bin/sudo") != 0)
        && (strcmp(name, "gksudo") != 0) 
        && (strcmp(name, "/bin/gksudo") != 0) 
        && (strcmp(name, "/usr/bin/gksudo") != 0) 
        && (strcmp(name, "kdesu") != 0) 
        && (strcmp(name, "/bin/kdesu") != 0) 
        && (strcmp(name, "/usr/bin/kdesu") != 0) 
       )
    {
        printf("Operation not permitted\n");
        kill(getpid(), SIGKILL);
        exit(1);
    }
}

#endif /* HARDENING */

#if !TRACEABLE

#define _LINUX_SOURCE_COMPAT
#include <sys/ptrace.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <fcntl.h>
#include <signal.h>
#include <stdio.h>
#include <unistd.h>

#if !defined(PT_ATTACHEXC) /* New replacement for PT_ATTACH */
   #if !defined(PTRACE_ATTACH) && defined(PT_ATTACH)
       #define PT_ATTACHEXC	PT_ATTACH
   #elif defined(PTRACE_ATTACH)
       #define PT_ATTACHEXC PTRACE_ATTACH
   #endif
#endif

void untraceable(char * argv0)
{
	char proc[80];
	int pid, mine;

	switch(pid = fork()) {
	case  0:
		pid = getppid();
		/* For problematic SunOS ptrace */
#if defined(__FreeBSD__)
		sprintf(proc, "/proc/%d/mem", (int)pid);
#else
		sprintf(proc, "/proc/%d/as",  (int)pid);
#endif
		close(0);
		mine = !open(proc, O_RDWR|O_EXCL);
		if (!mine && errno != EBUSY)
			mine = !ptrace(PT_ATTACHEXC, pid, 0, 0);
		if (mine) {
			kill(pid, SIGCONT);
		} else {
			perror(argv0);
			kill(pid, SIGKILL);
		}
		_exit(mine);
	case -1:
		break;
	default:
		if (pid == waitpid(pid, 0, 0))
			return;
	}
	perror(argv0);
	_exit(1);
}
#endif /* !TRACEABLE */

char * xsh(int argc, char ** argv)
{
	char * scrpt;
	int ret, i, j;
	char ** varg;
	char * me = argv[0];
	if (me == NULL) { me = getenv("_"); }
	if (me == 0) { fprintf(stderr, "E: neither argv[0] nor $_ works."); exit(1); }

	ret = chkenv(argc);
	stte_0();
	 key(pswd, pswd_z);
	arc4(msg1, msg1_z);
	arc4(date, date_z);
	if (date[0] && (atoll(date)<time(NULL)))
		return msg1;
	arc4(shll, shll_z);
	arc4(inlo, inlo_z);
	arc4(xecc, xecc_z);
	arc4(lsto, lsto_z);
	arc4(tst1, tst1_z);
	 key(tst1, tst1_z);
	arc4(chk1, chk1_z);
	if ((chk1_z != tst1_z) || memcmp(tst1, chk1, tst1_z))
		return tst1;
	arc4(msg2, msg2_z);
	if (ret < 0)
		return msg2;
	varg = (char **)calloc(argc + 10, sizeof(char *));
	if (!varg)
		return 0;
	if (ret) {
		arc4(rlax, rlax_z);
		if (!rlax[0] && key_with_file(shll))
			return shll;
		arc4(opts, opts_z);
#if HARDENING
	    arc4_hardrun(text, text_z);
	    exit(0);
       /* Seccomp Sandboxing - Start */
       seccomp_hardening();
#endif
		arc4(text, text_z);
		arc4(tst2, tst2_z);
		 key(tst2, tst2_z);
		arc4(chk2, chk2_z);
		if ((chk2_z != tst2_z) || memcmp(tst2, chk2, tst2_z))
			return tst2;
		/* Prepend hide_z spaces to script text to hide it. */
		scrpt = malloc(hide_z + text_z);
		if (!scrpt)
			return 0;
		memset(scrpt, (int) ' ', hide_z);
		memcpy(&scrpt[hide_z], text, text_z);
	} else {			/* Reexecute */
		if (*xecc) {
			scrpt = malloc(512);
			if (!scrpt)
				return 0;
			sprintf(scrpt, xecc, me);
		} else {
			scrpt = me;
		}
	}
	j = 0;
#if BUSYBOXON
	varg[j++] = "busybox";
	varg[j++] = "sh";
#else
	varg[j++] = argv[0];		/* My own name at execution */
#endif
	if (ret && *opts)
		varg[j++] = opts;	/* Options on 1st line of code */
	if (*inlo)
		varg[j++] = inlo;	/* Option introducing inline code */
	varg[j++] = scrpt;		/* The script itself */
	if (*lsto)
		varg[j++] = lsto;	/* Option meaning last option */
	i = (ret > 1) ? ret : 0;	/* Args numbering correction */
	while (i < argc)
		varg[j++] = argv[i++];	/* Main run-time arguments */
	varg[j] = 0;			/* NULL terminated array */
#if DEBUGEXEC
	debugexec(shll, j, varg);
#endif
	execvp(shll, varg);
	return shll;
}

int main(int argc, char ** argv)
{
#if SETUID
   setuid(0);
#endif
#if DEBUGEXEC
	debugexec("main", argc, argv);
#endif
#if HARDENING
	hardening();
#endif
#if !TRACEABLE
	untraceable(argv[0]);
#endif
	argv[1] = xsh(argc, argv);
	fprintf(stderr, "%s%s%s: %s\n", argv[0],
		errno ? ": " : "",
		errno ? strerror(errno) : "",
		argv[1] ? argv[1] : "<null>"
	);
	return 1;
}
